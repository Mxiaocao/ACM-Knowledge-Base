# 64 位随机哈希

## 简记

64 位随机哈希通常用 `unsigned long long` 存储随机值，碰撞概率较低，但不是数学上严格无碰撞。

## 在 C. Cool P #1 中

[[C. Cool P No.1|C. Cool P #1]] 的随机哈希练习版使用 64 位随机值表示质因子，再用 64 位 XOR 线性基维护 rank。

## 易错点

64 位线性基最多只有 64 个主元，真实质因子空间的 rank 可能超过 64，因此不能把它当成严格做法。