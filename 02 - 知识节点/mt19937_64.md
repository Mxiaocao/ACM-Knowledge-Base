# mt19937_64

## 简记

`mt19937_64` 是 C++ 标准库中的 64 位伪随机数生成器。

## 在 C. Cool P #1 中

[[C. Cool P No.1|C. Cool P #1]] 的随机哈希练习版用 `mt19937_64 rng(...)` 给每个质数生成一个 64 位随机哈希值。

## 易错点

随机种子会影响每次生成的值。调试时可以固定种子，但随机哈希本身仍不是严格无碰撞证明。