# 质因子集合哈希

## 简记

质因子集合哈希是把一个数中出现奇数次的质因子集合映射成哈希值。

## 在 C. Cool P #1 中

[[C. Cool P No.1|C. Cool P #1]] 的练习版中，如果 `60 = 2^2 * 3 * 5`，只保留 `3` 和 `5`，于是哈希值是 `h[3] ^ h[5]`。

## 易错点

它适合练习“奇数质因子集合 XOR”的思路，但不能替代严格的质因子向量表示。