# 递归专题二刷

## 基本信息

专题：递归专题基础学习  
首次学习日期：2026-07-14  
当前状态：未二刷  
学习状态：提示后完成  
掌握程度：大致理解但实现不稳  
正式提交验证：无  
是否回炉：否  

## 为什么需要二刷

本次完整知识结构和多数代码模板由教练讲解。

目前能够理解和解释主要概念，但尚未证明能够：

- 从零设计递归函数；
- 闭卷完成回溯；
- 独立实现 DFS；
- 独立写归并排序；
- 独立完成记忆化状态设计；
- 在正式题目中稳定通过。

## 二刷前禁止查看

- 全排列完整模板；
- 子集枚举完整模板；
- 归并排序完整合并代码；
- 图和网格 DFS 完整模板；
- 记忆化查询和保存的完整顺序；
- 当前递归专题完整笔记。

允许只查看题目描述和函数接口。

## 第一阶段：递归基础

独立完成：

1. 递归求 $1$ 到 $n$ 的和；
2. 阶乘；
3. 数组求和；
4. 数组最大值；
5. 判断数组是否有序；
6. 反转数组。

验收要求：

- 先写一句函数含义；
- 出口正确；
- 参数能够接近出口；
- 能解释返回后从哪里继续；
- 能分析时间和栈空间。

## 第二阶段：回溯

独立完成：

1. 输出所有二进制串；
2. 输出所有子集；
3. 输出所有排列；
4. 完成一个带基础剪枝的子集和问题。

验收要求：

- 能解释每个递归层决定什么；
- 能判断哪些状态需要恢复；
- 能解释 `push_back` 与 `pop_back` 的对应关系；
- 能手推至少一个新样例；
- 不混淆不同递归层的循环变量。

## 第三阶段：DFS

独立完成：

1. 树的深度；
2. 子树大小；
3. 无向图连通块数量；
4. 连通块大小；
5. 网格岛屿数量；
6. 最大连通区域。

验收要求：

- 能解释树中为什么用 `parent`；
- 能解释图中为什么用 `visited`；
- 不错误恢复图 DFS 的访问标记；
- 多测之间正确清空；
- 不发生数组越界。

## 第四阶段：分治与归并

独立完成：

1. 分治求区间和；
2. 分治求区间最大值；
3. 手写完整归并排序；
4. 手推一次双指针合并。

验收要求：

- 区分 `i、j、k` 的职责；
- 正确使用 `&&`；
- 正确复制剩余元素；
- 正确复制回原数组；
- 能说明 $O(n\log n)$ 的来源。

## 第五阶段：记忆化与 DP

独立完成：

1. 将普通斐波那契改为记忆化；
2. 将记忆化改为递推；
3. 使用滚动变量压缩；
4. 完成一个二维网格最大或最小路径问题；
5. 写出状态定义和遍历顺序。

验收要求：

- 先检查缓存；
- 未命中才递归；
- 正确保存答案；
- 遍历顺序满足依赖；
- 空间压缩不覆盖仍需使用的旧状态；
- 正确计算状态数量和转移数量。

## 达到什么标准才算已掌握

必须同时满足：

1. 不看模板完成上述核心实现；
2. 每个函数先写出明确含义；
3. 能解释出口和转移；
4. 能判断状态是否恢复；
5. 能手推新样例；
6. 能分析时间和空间复杂度；
7. 至少完成若干正式题目提交；
8. 没有依赖完整代码提示。

如果仍只能看懂、不能独立写出：

- 保持未二刷或改为二刷中；
- 不标记已掌握。

如果二刷后仍无法理解调用返回、状态恢复或函数定义：

- 改为回炉中；
- 先回到递归基础与调用栈。

## 关联知识节点

- [[递归]]
- [[回溯]]
- [[深度优先搜索]]
- [[分治]]
- [[归并排序]]
- [[记忆化搜索]]
- [[动态规划]]
- [[状态设计]]
- [[复杂度分析]]

## 专题应用题

以下题目作为递归专题的实战验收，二刷时应闭卷独立完成：

- [[E. Dominant Sequence]]：递归分治构造，返回区间和，中点负责人思想。当前回炉中。
